name: Chrome XFCE Desktop VNC + Playwright CDP
on: 
  workflow_dispatch:

jobs:
  chrome-user:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:22.04
      options: --privileged --shm-size=2gb
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. 安装基础环境 + XFCE4 + Chrome + Python Pip
      - name: Install System & Desktop & Chrome
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          apt-get update
          # 🔍 增加 python3-pip 用于安装 playwright
          apt-get install -y wget curl git net-tools sudo vim unzip xz-utils libgbm1 libnss3 libasound2 python3-pip
          
          # === 安装 Google Chrome ===
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          apt-get install -y ./google-chrome-stable_current_amd64.deb
          rm google-chrome-stable_current_amd64.deb
          
          # 安装字体
          apt-get install -y fonts-liberation fonts-noto-color-emoji fontconfig ttf-mscorefonts-installer
          
          # === 安装 XFCE4 ===
          apt-get install -y xvfb x11vnc python3 pulseaudio dbus-x11 xfce4 xfce4-goodies xfce4-terminal
          
          echo "ttf-mscorefonts-installer msttcorefonts/accepted-mscorefonts-eula select true" | debconf-set-selections
          fc-cache -f -v
          
          # 安装 noVNC
          git clone https://github.com/novnc/noVNC.git /opt/noVNC
          git clone https://github.com/novnc/websockify /opt/noVNC/utils/websockify
          ln -s /opt/noVNC/vnc.html /opt/noVNC/index.html
          chmod -R 777 /opt/noVNC

      # 2. 安装 Playwright 库 (不需要 install-deps 或 install chrome，因为我们只用 client)
      - name: Install Python Playwright
        run: |
          pip3 install playwright
          # 这一步不是必须的，因为我们用本地 Chrome，但安装它可以防止缺少某些底层依赖
          playwright install-deps chromium 

      # 3. 创建用户 & 配置 Chrome 反爬策略
      - name: Setup User & Chrome Preferences
        run: |
          useradd -m -s /bin/bash appuser
          echo "appuser ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers
          
          su appuser -c "mkdir -p /home/appuser/Downloads"
          su appuser -c "mkdir -p /home/appuser/Desktop"
          
          # === Chrome 核心配置 ===
          CONFIG_DIR="/home/appuser/.config/google-chrome/Default"
          su appuser -c "mkdir -p $CONFIG_DIR"
          
          su appuser -c "cat <<EOF > $CONFIG_DIR/Preferences
          {
            \"download\": {
              \"default_directory\": \"/home/appuser/Downloads\",
              \"prompt_for_download\": false,
              \"directory_upgrade\": true
            },
            \"savefile\": {
              \"default_directory\": \"/home/appuser/Downloads\"
            },
            \"profile\": {
              \"default_content_settings\": {
                \"popups\": 1,
                \"notifications\": 1
              },
              \"content_settings\": {
                \"exceptions\": {
                  \"automatic_downloads\": {
                    \"https://decrypt.day,*\": { \"setting\": 1 }
                  }
                }
              }
            },
            \"credentials_enable_service\": false,
            \"password_manager_enabled\": false
          }
          EOF"

      # 4. 启动 XFCE 桌面
      - name: Start XFCE Desktop
        run: |
          service dbus start || true
          
          cat <<EOF > /home/appuser/run_desktop.sh
          #!/bin/bash
          export DISPLAY=:99
          export HOME=/home/appuser
          
          Xvfb :99 -screen 0 1440x900x24 &
          sleep 2
          
          export XFCE_PANEL_MIGRATE_DEFAULT=true
          startxfce4 &
          
          x11vnc -display :99 -forever -bg -noxdamage
          /opt/noVNC/utils/novnc_proxy --vnc localhost:5900 --listen 6080 &
          EOF
          
          chmod +x /home/appuser/run_desktop.sh
          su appuser -c "/home/appuser/run_desktop.sh"
          
          sleep 5
          su appuser -c "export DISPLAY=:99; xset s off; xset -dpms; xset s noblank"

      # 5. 启动 Chrome (带防检测参数 + 调试端口)
      - name: Launch Chrome & Cloudflare Tunnel
        run: |
          cat <<EOF > /home/appuser/run_chrome.sh
          #!/bin/bash
          export DISPLAY=:99
          export HOME=/home/appuser
          
          # === 修正点 1: 增加 --remote-allow-origins=* ===
          # 新版 Chrome (v110+) 加强了安全性，WebSocket 连接必须显式允许来源
          
          nohup google-chrome-stable \\
            --no-first-run \\
            --no-default-browser-check \\
            --disable-infobars \\
            --disable-blink-features=AutomationControlled \\
            --password-store=basic \\
            --start-maximized \\
            --remote-debugging-port=9222 \\
            --remote-allow-origins=* \\
            --user-data-dir=/home/appuser/.config/google-chrome \\
            "https://decrypt.day/app/id1489932710" > /home/appuser/chrome.log 2>&1 &
          EOF
          
          chmod +x /home/appuser/run_chrome.sh
          su appuser -c "/home/appuser/run_chrome.sh"
          
          echo "Chrome launched command sent."

          # === 修正点 2: 主动等待端口开启 ===
          echo "Waiting for Chrome Debugging Port (9222) to open..."
          for i in $(seq 1 30); do
            # 使用 curl 测试接口是否响应 (比 nc 更通用)
            if curl -s http://127.0.0.1:9222/json/version > /dev/null; then
              echo "✅ Chrome Debugging Port is READY!"
              break
            fi
            echo "Waiting for port 9222... ($i/30)"
            sleep 1
            
            # 如果等了 5 秒还没好，看一眼日志有没有报错
            if [ "$i" -eq 5 ]; then
               echo "--- Chrome Log Preview (Early Check) ---"
               cat /home/appuser/chrome.log || true
               echo "--------------------------------------"
            fi
          done
          
          # Cloudflare Tunnel
          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          dpkg -i cloudflared.deb
          cloudflared tunnel --url http://localhost:6080 > tunnel.log 2>&1 &
          
          echo "Waiting for Tunnel..."
          for i in $(seq 1 10); do
            TUNNEL_URL=$(grep -oE "https://[a-zA-Z0-9-]+\.trycloudflare\.com" tunnel.log | head -1)
            if [ -n "$TUNNEL_URL" ]; then
                echo "Found Tunnel URL: $TUNNEL_URL"
                break
            fi
            sleep 1
          done
          
          echo "=========================================================="
          echo "🖥️  XFCE Desktop Ready!"
          echo "🔗  VNC Link: $TUNNEL_URL/vnc.html?autoconnect=true"
          echo "=========================================================="

      # 6. 🔍 新增步骤：Playwright CDP 连接验证
      - name: Run Playwright PoC Script
        run: |
          cat <<EOF > poc_script.py
          from playwright.sync_api import sync_playwright
          import time
          import sys

          def run():
              print(">>> Python Script Start")
              try:
                  with sync_playwright() as p:
                      # === 修正点 3: 强制使用 127.0.0.1 避免 IPv6 ::1 错误 ===
                      print(">>> Connecting to browser at http://127.0.0.1:9222 ...")
                      browser = p.chromium.connect_over_cdp("http://127.0.0.1:9222")
                      
                      context = browser.contexts[0]
                      if not context.pages:
                          print(">>> No pages found, creating one...")
                          page = context.new_page()
                      else:
                          page = context.pages[0]
                      
                      print(f">>> Success! Connected to page: {page.title()}")
                      
                      # 简单的验证动作
                      page.wait_for_timeout(2000)
                      page.screenshot(path="/home/appuser/Downloads/poc_proof.png")
                      print(">>> Screenshot saved.")
                      
                      browser.disconnect()
                      
              except Exception as e:
                  print(f"!!! Python Error: {e}")
                  sys.exit(1)

          if __name__ == "__main__":
              run()
          EOF

          tail -n 20 /home/appuser/chrome.log

          # 先检查一下端口还在不在
          if curl -s http://127.0.0.1:9222/json/version > /dev/null; then
              python3 poc_script.py
          else
              echo "❌ Port 9222 is closed! Chrome crashed?"
              echo "--- FINAL CHROME LOG ---"
              cat /home/appuser/chrome.log
              exit 1
          fi

          # 执行脚本 (注意：因为 Chrome 是 appuser 启动的，但端口是 localhost，root 也可以连)
          # 为了文件权限统一，建议用 su appuser 执行，或者 root 执行但注意写入路径
          # 这里直接运行 python3 即可
          
          su appuser -c "python3 poc_script.py"

      # 7. 监控下载 (保持不变)
      - name: Monitor Download (Wait for IPA)
        timeout-minutes: 30
        run: |
          TARGET_DIR="/home/appuser/Downloads"
          MAX_SIZE_MB=100
          
          echo "Starting monitoring loop in $TARGET_DIR"
          
          while true; do
            FILE=$(find "$TARGET_DIR" -name "*.ipa" -print -quit)
            
            if [ -n "$FILE" ]; then
              BASENAME=$(basename "$FILE")
              if [ -f "${FILE}.crdownload" ]; then
                 echo "⏳ Downloading (crdownload present): $BASENAME..."
              else
                 SIZE_MB=$(du -m "$FILE" | cut -f1)
                 if [ "$SIZE_MB" -le "$MAX_SIZE_MB" ]; then
                   echo "⚠️ Found $BASENAME ($SIZE_MB MB), waiting for full file..."
                 else
                   echo "✅ Success: $BASENAME ($SIZE_MB MB)"
                   break
                 fi
              fi
            else
               echo "Waiting for download..."
            fi
            sleep 5
          done
          
          echo "Download confirmed."

      # 8. 上传产物 (包含 IPA 和 PoC 截图)
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: downloaded-files
          path: |
            /home/appuser/Downloads/*.ipa
            /home/appuser/Downloads/*.png