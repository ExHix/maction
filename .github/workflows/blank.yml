name: Firefox Allowed-Ads VNC
on: 
  workflow_dispatch:

jobs:
  firefox-user:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:22.04
      options: --privileged --shm-size=2gb
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. 安装基础环境
      - name: Install System & Firefox
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          apt-get update
          apt-get install -y software-properties-common wget curl git net-tools sudo debconf-utils vim unzip
          
          add-apt-repository -y ppa:mozillateam/ppa
          
          echo '
          Package: *
          Pin: release o=LP-PPA-mozillateam
          Pin-Priority: 1001
          ' | tee /etc/apt/preferences.d/mozilla-firefox
          
          apt-get update
          apt-get install -y firefox fonts-liberation fonts-noto-color-emoji fontconfig ttf-mscorefonts-installer xvfb fluxbox x11vnc python3 pulseaudio dbus-x11
          
          echo "ttf-mscorefonts-installer msttcorefonts/accepted-mscorefonts-eula select true" | debconf-set-selections
          fc-cache -f -v
          
          git clone https://github.com/novnc/noVNC.git /opt/noVNC
          git clone https://github.com/novnc/websockify /opt/noVNC/utils/websockify
          ln -s /opt/noVNC/vnc.html /opt/noVNC/index.html
          chmod -R 777 /opt/noVNC

      # 2. 创建用户 & 配置“允许广告”的 Profile
      - name: Setup User & Firefox Profile
        run: |
          useradd -m -s /bin/bash appuser
          
          su appuser -c "mkdir -p /home/appuser/.mozilla/firefox/stealth.default"
          
          su appuser -c "cat <<EOF > /home/appuser/.mozilla/firefox/profiles.ini
          [Profile0]
          Name=default
          IsRelative=1
          Path=stealth.default
          Default=1
          
          [General]
          StartWithLastProfile=1
          Version=2
          EOF"
          
          # 核心修改：写入允许广告和追踪的 user.js
          su appuser -c "cat <<EOF > /home/appuser/.mozilla/firefox/stealth.default/user.js
          // 1. 基础反爬 (保持不变)
          user_pref(\"dom.webdriver.enabled\", false);
          user_pref(\"use_automation_extension\", false);
          user_pref(\"general.useragent.override\", \"Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:120.0) Gecko/20100101 Firefox/120.0\");
          
          // 2. 必须禁用的隐私保护 (修复 AdBlock 检测)
          // 关闭增强跟踪保护
          user_pref(\"privacy.trackingprotection.enabled\", false);
          user_pref(\"privacy.trackingprotection.pbmode.enabled\", false);
          user_pref(\"privacy.trackingprotection.socialtracking.enabled\", false);
          user_pref(\"privacy.trackingprotection.cryptomining.enabled\", false);
          user_pref(\"privacy.trackingprotection.fingerprinting.enabled\", false);
          
          // 允许所有 Cookie (很多广告依赖第三方 Cookie)
          user_pref(\"network.cookie.cookieBehavior\", 0); // 0 = Accept all
          user_pref(\"network.cookie.thirdparty.sessionOnly\", false);
          
          // 3. 其他可能触发检测的设置
          user_pref(\"dom.popup_allowed_events\", \"change click dblclick mouseup notificationclick reset submit touchend\"); 
          user_pref(\"dom.disable_open_during_load\", false); // 允许弹窗 (有些广告是弹窗)
          
          // 4. 保持 WebRTC 关闭 (防止 IP 泄露，如果广告一定需要 WebRTC 才能加载，这里可能需要改回 true)
          user_pref(\"media.peerconnection.enabled\", false);
          EOF"

      # 3. 启动桌面
      - name: Start Desktop (As User)
        run: |
          service dbus start || true
          
          cat <<EOF > /home/appuser/run_desktop.sh
          #!/bin/bash
          export DISPLAY=:99
          export HOME=/home/appuser
          Xvfb :99 -screen 0 1280x800x24 &
          sleep 2
          fluxbox &
          x11vnc -display :99 -forever -passwd secret -bg -noxdamage
          /opt/noVNC/utils/novnc_proxy --vnc localhost:5900 --listen 6080 &
          EOF
          
          chmod +x /home/appuser/run_desktop.sh
          su appuser -c "/home/appuser/run_desktop.sh"
          sleep 3

      # 4. 启动 Firefox
      - name: Launch Firefox & Tunnel
        run: |
          cat <<EOF > /home/appuser/run_firefox.sh
          #!/bin/bash
          export DISPLAY=:99
          export HOME=/home/appuser
          # 替换成你的目标网址
          nohup firefox --no-remote --new-instance "https://nowsecure.nl" > /home/appuser/firefox.log 2>&1 &
          EOF
          
          chmod +x /home/appuser/run_firefox.sh
          su appuser -c "/home/appuser/run_firefox.sh"
          
          echo "Firefox launching..."
          sleep 5
          
          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          dpkg -i cloudflared.deb
          cloudflared tunnel --url http://localhost:6080 > tunnel.log 2>&1 &
          
          echo "Waiting for Tunnel..."
          sleep 10
          
          TUNNEL_URL=$(grep -oE "https://[a-zA-Z0-9-]+\.trycloudflare\.com" tunnel.log | head -1)
          
          echo "=========================================================="
          echo "🦊 Firefox (Ad-Friendly Mode) 已启动"
          echo "🔗 VNC 链接: $TUNNEL_URL/vnc.html?autoconnect=true&password=secret"
          echo "=========================================================="
          
          # tail -f /home/appuser/firefox.log
    
      # 5. 核心步骤：监控下载并校验
      - name: Monitor Download (Wait for IPA)
        run: |
          TARGET_DIR="/home/appuser/Downloads"
          MAX_SIZE_MB=100
          
          echo "Starting monitoring loop for *.ipa in $TARGET_DIR"
          
          while true; do
            # 查找 ipa 文件 (不包含 .part 临时文件)
            FILE=$(find "$TARGET_DIR" -name "*.ipa" -print -quit)
            
            if [ -n "$FILE" ]; then
              BASENAME=$(basename "$FILE")
              
              # 1. 检查是否还在下载 (Firefox 通常会生成 .part)
              if [ -f "${FILE}.part" ] || [ -f "${FILE}.partjs" ]; then
                 echo "⏳ File $BASENAME is still downloading..."
              else
                 # 2. 检查大小
                 SIZE_MB=$(du -m "$FILE" | cut -f1)
                 
                 if [ "$SIZE_MB" -ge "$MAX_SIZE_MB" ]; then
                   echo "⚠️ Found $BASENAME but size ($SIZE_MB MB) is too large (> $MAX_SIZE_MB MB). Waiting for correct file..."
                 else
                   echo "✅ Found candidate: $BASENAME ($SIZE_MB MB)"
                   
                   # 3. 检查完整性 (使用 zip -T)
                   echo "🔍 Verifying integrity..."
                   if zip -T "$FILE" > /dev/null 2>&1; then
                     echo "🎉 Integrity Check PASSED!"
                     echo "FILE_PATH=$FILE" >> $GITHUB_ENV
                     break
                   else
                     echo "❌ Integrity Check FAILED. The file might be incomplete or corrupted. Waiting..."
                   fi
                 fi
              fi
            else
               # 没有任何文件时，仅打印心跳
               echo "Waiting for user to download IPA..."
            fi
            
            sleep 3
          done
          
          echo "Download complete and verified. Stopping session."

      # 6. 上传产物
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: decrypted-app
