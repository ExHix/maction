name: MacOS System Info Check

# 设置触发条件：推送到 main 分支或手动触发
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  check-system:
    # 使用最新版本的 macOS 运行环境
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print Hello World and System Info
        shell: bash
        run: |
          echo "-------------------------------"
          echo "🚀 Hello World from GitHub Actions!"
          echo "-------------------------------"
          
          echo "📍 Current User: $(whoami)"
          echo "💻 System Version:"
          sw_vers
          
          echo -e "\n📂 Hardware Overview:"
          system_profiler SPHardwareDataType | grep -E "Model Name|Processor Name|Total Number of Cores|Memory"
          
          echo -e "\n🌐 Network Config:"
          ifconfig en0 | grep "status"
          
          echo -e "\n📅 Current Date & Time:"
          date
          echo "-------------------------------"
